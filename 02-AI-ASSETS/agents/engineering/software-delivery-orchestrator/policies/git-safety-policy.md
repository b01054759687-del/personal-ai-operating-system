# Policy: Git Safety & Worktree Protection
Never execute destructive operations (git reset --hard, git clean -fd). Always isolate work in feature branches. Never commit directly to main. Quarantining unrelated user work is mandatory.
